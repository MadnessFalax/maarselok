<Window x:Class="desktop_client.StudentEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:desktop_client"
        mc:Ignorable="d"
        Title="Student Editor" Height="400" Width="500">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*">

            </RowDefinition>
            <RowDefinition Height="auto">

            </RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel x:Name="ControlPanel" Grid.Row="0" Background="White" Margin="5">

        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button Style="{StaticResource RoundedButtonStyle}" Width="50"  Height="25" Margin="5" Click="OkCallback">
                Ok
            </Button>
            <Button Style="{StaticResource RoundedButtonStyle}" Width="50" Height="25" Margin="5" Click="CancelCallback">
                Cancel
            </Button>
        </StackPanel>
    </Grid>
</Window>
